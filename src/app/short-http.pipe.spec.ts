import { ShortHTTPPipe } from './short-http.pipe';

describe('ShortHTTPPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortHTTPPipe();
    expect(pipe).toBeTruthy();
  });
});
